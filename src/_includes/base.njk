<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <link rel="canonical" href="https://{{ client.domain }}{{ page.url }}" />

  <meta name="description" content="{{ description | default('') }}" />

  <meta property="og:title" content="{{ metaTitle | default(tagTitle) }}" />
  <meta property="og:description" content="{{ description | default('') }}" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://{{ client.domain }}{{ page.url }}" />
  <meta property="og:image" content="/images/social.jpg" />

  <link rel="icon" href="/favicon.ico" />

  <link rel="stylesheet" href="/css/root.css" />
  <link rel="stylesheet" href="/css/critical.css" />

  {% if additionalCSS %}
    <link rel="stylesheet" href="{{ additionalCSS }}">
  {% endif %}

  <title>
    {% if page.url == "/" %}
      {{ client.name }} | {{ tagTitle }}
    {% else %}
      {{ tagTitle }} | {{ client.name }}
    {% endif %}
  </title>
</head>

<body>
  <a class="skip" href="#main">Skip to content</a>

  {% include "header.njk" %}

  <main id="main">
    {{ content | safe }}
  </main>

  {% include "footer.njk" %}

  <script defer src="/assets/js/nav.js"></script>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{{ title or "KV Garage" }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>
